{
  "name": "sobr",
  "version": "0.1.0-dev",
  "description": "sobr — the coding agent that refuses to let you vibecode. Teaching-first, glass-box agent runtime.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "sobr": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
