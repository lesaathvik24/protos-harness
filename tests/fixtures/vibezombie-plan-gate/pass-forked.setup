# active (this session) + a ## FORK logged after the session marker → plan allowed
mkdir -p "$VIBEZOMBIE_DIR/sessions/sess-A"
printf 'L2 hard\n' > "$VIBEZOMBIE_DIR/sessions/sess-A/active"
printf '# vibezombie decision log\n\n<!-- vz-session 2026-06-06T09:00:00Z -->\n## FORK — stack for the app\n- chose: Next + Postgres\n' > "$VIBEZOMBIE_DIR/log.md"
